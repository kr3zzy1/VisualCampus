export type Category='campus'|'dormitory'|'classroom'|'library'|'city'|'sports'|'laboratories'|'student_life';export interface University{name:string;country?:string|null;region?:string|null;website?:string|null;domain?:string|null}export interface Photo{id:string;thumbnail_url:string;source_url:string;title:string;category:Category;confidence:number;status:string;reasons:string[]}export interface Profile{university:University;summary:string;categories:Record<Category,Photo[]>;warnings:string[]}export interface Job{id:string;status:'processing'|'completed'|'failed'|'ambiguous';progress:string[];candidates:University[];profile?:Profile;error?:string}
